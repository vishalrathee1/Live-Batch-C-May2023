#include <iostream>
using namespace std;

int main(){
	int i = 0,j=5,k=-2;
	(i>1)? ( (j<=5) ? cout<<"Hello There!"<<endl : cout<<"Woof"<<endl ): ( (k>=2) ? cout<<"Meow"<<endl : cout<<"Apples"<<endl );
	return 0;
}
